# SteadyDancer Worker app environment

# 模型根目录（通常继承全局 MODELS_DIR）
MODELS_DIR=./models

# 可选：覆盖 SteadyDancer 权重目录
# 如不设置，则默认使用 ${MODELS_DIR}/SteadyDancer-14B
STEADYDANCER_CKPT_DIR=

# Celery 队列配置
# 推荐显式使用 Celery 标准环境变量，JOB_QUEUE_URL 作为兼容别名。
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2
CELERY_DEFAULT_QUEUE=steadydancer
WORKER_CONCURRENCY=4
JOB_QUEUE_URL=redis://localhost:6379/1
