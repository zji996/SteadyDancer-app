# Worker 专属环境变量示例
# - 模型与 DF11 相关的环境变量（MODELS_DIR / STEADYDANCER_CKPT_DIR / STEADYDANCER_DF11_*）
#   统一在仓库根 .env / .env.example 中维护；如需针对 worker 覆盖，可在 apps/worker/.env 中设置同名变量。

# Celery 队列配置
# 推荐显式使用 Celery 标准环境变量，JOB_QUEUE_URL 作为兼容别名。
CELERY_DEFAULT_QUEUE=steadydancer
WORKER_CONCURRENCY=4
JOB_QUEUE_URL=redis://localhost:6379/1
