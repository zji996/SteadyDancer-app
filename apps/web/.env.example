# Web app environment variables (Vite)

# API 服务基础地址
VITE_API_BASE_URL=http://192.168.8.12:8000

# 可选：前端调用后端时使用的 API Key
# - 对应后端环境变量 STEADYDANCER_API_KEY 的值（同一串字符串，前端 / 后端各自设置）；
# - 推荐做法：
#   - 在仓库根 .env 中设置 STEADYDANCER_API_KEY；
#   - 在本文件对应的 apps/web/.env 中设置 VITE_API_KEY，并保持与后端 Key 一致；
# - 请求时应在 Header 中加上：X-API-Key: ${VITE_API_KEY}
VITE_API_KEY=
