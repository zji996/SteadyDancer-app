# SteadyDancer API app environment
# API 专属配置
# API 监听地址与端口
API_HOST=0.0.0.0
API_PORT=8000

# 数据库自动建表行为由 STEADYDANCER_DB_AUTO_CREATE 控制（见仓库根 .env.example）

# 可选：为 API 配置专用的 API Key（优先级同样依赖进程环境）
# 一般推荐在仓库根 .env 中统一设置 STEADYDANCER_API_KEY，
# 如需针对 API 单独覆盖，也可以在 apps/api/.env 中设置。
#STEADYDANCER_API_KEY=
