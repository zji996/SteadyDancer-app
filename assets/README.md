# assets/

静态资源与示例素材目录。

- 可放置演示用图片、视频、配置示例等。
- 不建议放置体积过大的二进制文件，如需使用请考虑外部存储或按需下载。

特别约定：

- 业务运行数据（Project / Job 的输入/输出等）统一落在仓库根目录的 `data/` 下（见环境变量 `DATA_DIR` / `STEADYDANCER_DATA_DIR` 的默认值），
  而非本目录；`assets/` 更多用于静态资源与示例素材。
